version: '3.9'
services:
  app: 
    build:
      context: .
    ports:
      - "8000:8000"
      
# services:
#   product_api:
#     container_name: product_api
#     build: 
#       context: .
#       dockerfile: Dockerfile
#     ports:
#       - "8000:8000"
    